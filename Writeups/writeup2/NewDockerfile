FROM dagnyr/bioinfo_example:amd64

RUN pip install parasail

RUN curl -L https://github.com/rcedgar/reseek/releases/download/v2.7/reseek-v2.7-linux-x86 \
     -o /usr/local/bin/reseek && \
    chmod +x /usr/local/bin/reseek

